'use strict';

var config = {
    name: 'scripts:test-devmode',
    description: 'Run tests in dev path',
    config: 'tests',
    callback: callback
};

module.exports = config;

/////////////////////////////

function callback() {

}
