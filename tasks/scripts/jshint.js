'use strict';

var config = {
    name: 'scripts:jshint',
    description: 'Lint js code',
    callback: callback,

    settings: {
        'name': '${name}'
    }

};

module.exports = config;

/////////////////////////////

function callback() {

}
